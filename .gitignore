# build output (auto-generated by scripts/build.js — কখনো নিজে বানাবেন না)
www/
node_modules/

# ── Capacitor native project — CI (build-apk.yml) প্রতিবার নতুন generate করে
#    (npx cap add android), তাই পুরো ফোল্ডারটাই বাদ। ভুলে কমিট করলে
#    কিস্টোর/google-services.json এর মতো sensitive ফাইলও চলে যেতে পারত। ──
android/
ios/

# ── Sensitive/secret ফাইল — কখনো কমিট করা উচিত না ──
*.keystore
*.jks
google-services.json
serviceAccountKey.json
firebase-service-account*.json
.env
.env.local

# ── সাধারণ housekeeping ──
*.log
.DS_Store
Thumbs.db
dist/
